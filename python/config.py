import numpy as np

mode = True # Konfiguration um Systemweite Kameramodus zu haben

mask_values = [
    [np.array([0, 0, 0]), np.array([0, 0, 0])],
    [np.array([0, 0, 0]), np.array([0, 0, 0])],
    [np.array([0, 0, 0]), np.array([0, 0, 0])]
]